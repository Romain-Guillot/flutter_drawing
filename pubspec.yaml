name: drawing
description: A new Flutter package project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_icons: ^1.0.0+1
  selectors:
    git:
      url: git@github.com:Romain-Guillot/flutter_selectors.git
      ref: master


flutter:
